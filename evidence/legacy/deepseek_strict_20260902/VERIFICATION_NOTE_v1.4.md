# v1.4 verification note

The original `SHA256_MANIFEST.csv` protects the historical evidence bundle as it was captured. The two `verified_*.json` files at this directory level are **new v1.4 classification summaries** and are intentionally not presented as members of that historical manifest.

Under v1.4 they are classified `LEGACY_REAL_API_EVIDENCE_REQUIRES_CURRENT_RERUN`: historical DeepSeek request/provider/usage facts remain useful, but the old run lacks the newer transport and OR-Tools solver-provenance fields required for the current strict verdict.
